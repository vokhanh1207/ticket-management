export enum TicketStatus {
    Active = 'CREATED',
    CheckedIn = 'CHECKED-IN',
    Used = 'USED',
    Expired = 'EXPIRED',
}

export enum TicketAction {
    CheckIn = 'check-in',
    CheckOut = 'check-out'
}

