export declare enum TicketStatus {
    Active = "CREATED",
    CheckedIn = "CHECKED-IN",
    Used = "USED",
    Expired = "EXPIRED"
}
export declare enum TicketAction {
    CheckIn = "check-in",
    CheckOut = "check-out"
}
